# Per-region authoring source for Wilderness.
# One bare chunk object per file, e.g. Wilderness_Name_<region_id>.json
# Human-categorization layer; the plugin loads All_Areas_Task_Folder/Wilderness_Tasks.json (mirrored to src).
